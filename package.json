{
  "name": "api_hunt",
  "version": "1.0.0",
  "description": "API Hunt is a web app that you can browse free api's for your next projects. using MERN stack",
  "main": "index.js",
  "scripts": {
    "build": "cd backend && npm install  && cd ../frontend && npm install && npm run build",
    "start":"cd backend && node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
